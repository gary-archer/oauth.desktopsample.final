1. Login navigation is not right yet
   App gets recreated after Refresh Access Token + Home -> history.go
   Make history.push work correctly

2. RendererEvents needs to be given the hashHistory, but this is a view lifecycle object.
   Currently RendererEvents is managed by the view model.
   Raising an onDeepLinked view event might be simplest?

3. SPA
   HeaderbuttonsView to use type='button'
   Same change to get rid of history.go