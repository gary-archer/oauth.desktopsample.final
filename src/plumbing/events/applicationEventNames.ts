/*
 * Named events for the UI
 */
export class ApplicationEventNames {

    // Sent to ask the main view to reload itself
    public static readonly ON_RELOAD_MAIN = 'reload_main';

    // Sent to ask the user info view to reload itself
    public static readonly ON_RELOAD_USERINFO = 'reload_userinfo';
}
